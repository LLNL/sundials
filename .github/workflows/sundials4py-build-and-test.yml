name: sundials4py build-and-test

on:
  # Allows this workflow to be called by other workflows
  workflow_call:
  # Allows manual triggering
  workflow_dispatch:

# Cancel any in-progress workflow runs
concurrency:
  group: sundials4py-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  sundials4py-build-sdist:
    name: Build SDist
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
      with:
        submodules: true

    - name: Build SDist
      run: pipx run build --sdist

    - name: Check metadata
      run: pipx run twine check dist/*

    - uses: actions/upload-artifact@v7
      with:
        name: dist-sdist
        path: dist/*.tar.gz

  sundials4py-build-wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, macos-15-intel, windows-latest]

    steps:
    - name: Set macOS deployment target
      if: contains(matrix.os, 'macos')
      run: echo "MACOSX_DEPLOYMENT_TARGET=$(sw_vers -productVersion | cut -d '.' -f 1-2)" >> "$GITHUB_ENV"

    - uses: actions/checkout@v7
      with:
        submodules: true

    - uses: pypa/cibuildwheel@v4.2
      env:
        # Default (Linux/macOS): install matplotlib and run both test suites
        CIBW_TEST_REQUIRES: "pytest matplotlib"
        CIBW_TEST_COMMAND: "pytest {project}/bindings/sundials4py/test {project}/examples/python"
        # Windows: DON'T install matplotlib, and skip the examples
        CIBW_TEST_REQUIRES_WINDOWS: "pytest"
        CIBW_TEST_COMMAND_WINDOWS: "pytest {project}/bindings/sundials4py/test"

    - name: Upload wheels
      uses: actions/upload-artifact@v7
      with:
        path: wheelhouse/*.whl
        name: dist-${{ matrix.os }}

  sundials4py-test-array-backends:
    name: Test CPU array backends
    uses: ./.github/workflows/sundials4py-test-array-backends.yml

  sundials4py-build-wheels-extra:
    name: Build extra wheels on ${{ matrix.os }} (${{ matrix.precision }}/${{ matrix.index }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        precision: [single, double]
        index: [32, 64]

    steps:
    - name: Set macOS deployment target
      if: contains(matrix.os, 'macos')
      run: echo "MACOSX_DEPLOYMENT_TARGET=$(sw_vers -productVersion | cut -d '.' -f 1-2)" >> "$GITHUB_ENV"

    - uses: actions/checkout@v7
      with:
        submodules: true

    # Double precision - run all tests
    - uses: pypa/cibuildwheel@v4.2
      if: matrix.precision == 'double'
      env:
        CMAKE_ARGS: "-DSUNDIALS_PRECISION=${{ matrix.precision }} -DSUNDIALS_INDEX_SIZE=${{ matrix.index }}"
        CIBW_TEST_REQUIRES: "pytest matplotlib"
        CIBW_TEST_COMMAND: "pytest {project}/bindings/sundials4py/test {project}/examples/python"

    # Single precision - skip examples
    - uses: pypa/cibuildwheel@v4.2
      if: matrix.precision == 'single'
      env:
        CMAKE_ARGS: "-DSUNDIALS_PRECISION=${{ matrix.precision }} -DSUNDIALS_INDEX_SIZE=${{ matrix.index }}"
        CIBW_TEST_REQUIRES: "pytest"
        CIBW_TEST_COMMAND: "pytest {project}/bindings/sundials4py/test"

    - name: Rename wheels to include config
      run: |
        for whl in wheelhouse/*.whl; do
          mv "$whl" "${whl%.whl}-${{ matrix.precision }}-${{ matrix.index }}.whl"
        done
        ls -al wheelhouse/
      shell: bash

    - name: Upload wheels as artifacts
      uses: actions/upload-artifact@v7
      with:
        path: wheelhouse/*.whl
        name: extra-dist-${{ matrix.os }}-${{ matrix.precision }}-${{ matrix.index }}

  sundials4py-test-klu:
    name: Test sundials4py with KLU
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
      with:
        submodules: true

    - name: Install KLU
      run: |
        sudo apt-get update
        sudo apt-get install --no-install-recommends -y libsuitesparse-dev

    - name: Setup Python
      uses: actions/setup-python@v7
      with:
        python-version: '3.14'

    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        pip install build scikit-build-core nanobind numpy pytest

    - name: Build wheel
      env:
        CMAKE_ARGS: "-DSUNDIALS_ENABLE_KLU=ON"
      run: python -m build -o wheelhouse

    - name: Install wheel
      run: pip install wheelhouse/*.whl --force-reinstall

    - name: Run KLU example
      run: python examples/python/arkode/ark_brusselator1D_klu.py
