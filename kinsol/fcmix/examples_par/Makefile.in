# -----------------------------------------------------------------
# $Revision: 1.4 $
# $Date: 2004-10-21 21:12:33 $
# -----------------------------------------------------------------
# Programmer(s): Radu Serban and Aaron Collier @ LLNL
# -----------------------------------------------------------------
# Copyright (c) 2002, The Regents of the University of California.
# Produced at the Lawrence Livermore National Laboratory.
# All rights reserved.
# For details, see sundials/kinsol/LICENSE.
# -----------------------------------------------------------------
# Makefile for FKINSOL parallel examples
#
# @configure_input@
# -----------------------------------------------------------------

SHELL = @SHELL@

srcdir       = @srcdir@
top_builddir = @top_builddir@
builddir     = @builddir@
prefix       = @prefix@
exec_prefix  = @exec_prefix@
includedir   = @includedir@
libdir       = @libdir@

INSTALL = @INSTALL@

LIBTOOL = @LIBTOOL@

FFLAGS      = @FFLAGS@
LDFLAGS     = @LDFLAGS@
LIBS        = @LIBS@
FLIBS       = @FLIBS@
MPIF77      = @MPIF77@
MPI_INC_DIR = @MPI_INC_DIR@
MPI_LIB_DIR = @MPI_LIB_DIR@
MPI_LIBS    = @MPI_LIBS@

SUNDIALS_LIB_DIR = $(libdir)
SUNDIALS_LIBS    = -lsundials_fkinsol -lsundials_kinsol -lsundials_fnvecparallel -lsundials_nvecparallel -lsundials_shared

SOURCE_FILES = kindiagpf.f
EXEC_FILES   = kindiagpf

all: update $(EXEC_FILES)

update:
	@${INSTALL} ${top_builddir}/config/fortran_update ${builddir}/fortran_update.sh

kindiagpf: $(srcdir)/kindiagpf.f
	@${SHELL} ${builddir}/fortran_update.sh ${srcdir} kindiagpf.f
	$(LIBTOOL) --mode=compile $(MPIF77) -I$(MPI_INC_DIR) $(FFLAGS) -c $(builddir)/kindiagpf-updated.f
	$(LIBTOOL) --mode=link $(MPIF77) -o $(builddir)/kindiagpf $(builddir)/kindiagpf-updated.lo $(LDFLAGS) $(LIBS) $(FLIBS) -L$(SUNDIALS_LIB_DIR) $(SUNDIALS_LIBS) -L$(MPI_LIB_DIR) $(MPI_LIBS)

clean:
	@for i in ${EXEC_FILES} ; do             \
	   $(LIBTOOL) --mode=clean rm -f $${i} ; \
	   rm -f $${i}-updated.f ;               \
	   rm -f $${i}-updated.lo ;              \
	   rm -f $${i}-updated.o ;               \
	 done
	rm -f fortran_update.sh
