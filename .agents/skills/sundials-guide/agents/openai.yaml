interface:
  display_name: "SUNDIALS Guide"
  short_description: "Choose, implement, validate, and tune a SUNDIALS solver stack"
  default_prompt: "Use $sundials-guide to choose and implement a SUNDIALS solver stack, validate it, and autotune supported SetOptions parameters with suntools tune while accounting for problem- and machine-specific results."
