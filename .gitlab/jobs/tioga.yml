##############################################################################
# Copyright (c) 2022-24, Lawrence Livermore National Security, LLC and RADIUSS
# project contributors. See the COPYRIGHT file for details.
#
# SPDX-License-Identifier: (MIT)
##############################################################################
# ------------------------------------------------------------------------------
# SUNDIALS Copyright Start
# Copyright (c) 2002-2024, Lawrence Livermore National Security
# and Southern Methodist University.
# All rights reserved.
#
# See the top-level LICENSE and NOTICE files for details.
#
# SPDX-License-Identifier: BSD-3-Clause
# SUNDIALS Copyright End
# ------------------------------------------------------------------------------

##########################################
# Overridden shared radiuss-shared-ci jobs
##########################################

# We duplicate the shared jobs description and add necessary changes for this
# project. We keep ${PROJECT_<MACHINE>_VARIANTS} and ${PROJECT_<MACHINE>_DEPS}
# So that the comparison with the original job is easier.

# No overridden jobs so far.

########################
# Extra jobs
########################

.sundials_job_on_tioga:
  variables:
    MPIEXEC_EXECUTABLE: "flux"
    MPIEXEC_PREFLAGS: "run"
  extends: [.custom_job, .job_on_tioga]

# --- Builds with HIP

# tioga_rocmcc_571_no_tpls:
#   parallel:
#     matrix:
#       - COMPILER_SPEC: rocmcc@5.7.1
#         AMDGPU_TARGET: [gfx90a]
#   variables:
#     SPEC: "%${COMPILER_SPEC} cstd=99 cxxstd=14 precision=double +rocm~mpi~magma~ginkgo~kokkos amdgpu_target=${AMDGPU_TARGET}"
#   before_script:
#     - module load rocmcc/5.7.1-magic
#   extends: [.sundials_job_on_tioga]

tioga_rocmcc_571_tpls:
  parallel:
    matrix:
      - COMPILER_SPEC: rocmcc@5.7.1
        AMDGPU_TARGET: [gfx90a]
  variables:
    SPEC: "%${COMPILER_SPEC} cstd=99 cxxstd=14 precision=double amdgpu_target=${AMDGPU_TARGET} +rocm+mpi+magma+ginkgo+kokkos ^magma+rocm amdgpu_target=${AMDGPU_TARGET} ^ginkgo+rocm amdgpu_target=${AMDGPU_TARGET} ^kokkos+rocm amdgpu_target=${AMDGPU_TARGET}"
  before_script:
    - module load rocmcc/5.7.1-magic
  extends: [.sundials_job_on_tioga]

# tioga_rocmcc_620_no_tpls:
  parallel:
    matrix:
      - COMPILER_SPEC: rocmcc@6.2.0
        AMDGPU_TARGET: [gfx90a]
  variables:
    SPEC: "%${COMPILER_SPEC} cstd=99 cxxstd=14 precision=double +rocm~mpi~magma~ginkgo~kokkos amdgpu_target=${AMDGPU_TARGET}"
  before_script:
    - module load rocmcc/6.2.0-magic
  extends: [.sundials_job_on_tioga]

tioga_rocmcc_620_tpls:
  parallel:
    matrix:
      - COMPILER_SPEC: rocmcc@6.2.0
        AMDGPU_TARGET: [gfx90a]
  variables:
    SPEC: "%${COMPILER_SPEC} cstd=99 cxxstd=14 precision=double amdgpu_target=${AMDGPU_TARGET} +rocm+mpi+magma~ginkgo+kokkos ^magma+rocm amdgpu_target=${AMDGPU_TARGET} ^kokkos+rocm amdgpu_target=${AMDGPU_TARGET}"
  before_script:
    - module load rocmcc/6.2.0-magic
  extends: [.sundials_job_on_tioga]

# --- Builds without HIP

tioga_cce_1800_mpi_only:
  parallel:
    matrix:
      - COMPILER_SPEC: cce@18.0.0
  variables:
    SPEC: "%${COMPILER_SPEC} cstd=99 cxxstd=14 precision=double +mpi"
  extends: [.sundials_job_on_tioga]


# --- Benchmark jobs

tioga_rocmcc_620_benchmark:
  parallel:
    matrix:
      - COMPILER_SPEC: rocmcc@6.2.0
        AMDGPU_TARGET: [gfx90a]
        CALIPER_DIR: /usr/workspace/sundials/califiles
  variables:
    SPEC: "%${COMPILER_SPEC} cstd=99 cxxstd=14 build_type=Release precision=double scheduler=lsf caliper-dir=${CALIPER_DIR} ~int64 +benchmarks+profiling+caliper+adiak+mpi+openmp+cuda+raja cuda_arch=70 ^raja+cuda~openmp~examples~exercises cuda_arch=70 ^caliper+adiak+cuda cuda_arch=70 ^${CUDA_SPEC}"
    ON_TIOGA: OFF # turn this job off for now
  extends: [.sundials_job_on_tioga]
