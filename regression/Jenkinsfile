#!/usr/bin/env groovy
pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
              echo "Running tests as..."           
              sh('whoami')
              echo "Build and Test..."
              sh('cd regression && ./suntest_driver.sh')
            }
        }
    }
}
